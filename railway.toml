[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/health"

[[crons]]
schedule = "0 3 * * *"
command = "curl -X POST https://$RAILWAY_PUBLIC_DOMAIN/api/sync-jobs -H 'x-sync-secret: $SYNC_SECRET' -H 'x-triggered-by: cron'"
